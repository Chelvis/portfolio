export class metaData {
  title: string;
  description: string;
  url: string;
  keywords: string;

  constructor(
    title,
    description,
    url,
    keywords
  ) {
    this.title = title;
    this.description = description;
    this.url = url;
    this.keywords = keywords;
  }

}
